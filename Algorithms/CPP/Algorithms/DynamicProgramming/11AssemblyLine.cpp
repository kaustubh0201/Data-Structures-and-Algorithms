#include<iostream>
#include<vector>

using namespace std;

int assemblyLineScheduling(vector<vector<int>> &assembleTime, vector<int> &turningTime, vector<int> &entryTime, vector<int> &exitTime){
    
}

int main(void){

    vector<vector<int>> assembleTime = {{4, 5, 3, 2}, {2, 10, 1, 4}};
    vector<vector<int>> turningTime = {{0, 7, 4, 5}, {0, 9, 2, 8}};
    vector<int> entryTime = {10, 12};
    vector<int> exitTime = {18, 7};


    return 0;
}